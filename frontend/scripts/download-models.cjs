/**
 * download-models.cjs
 * Previously used to download @imgly/background-removal models.
 * Now replaced by @huggingface/transformers (RMBG-1.4) which downloads
 * its model automatically on first use and caches it in the browser.
 *
 * This script is kept as a placeholder but performs no operations.
 */
console.log("✅ Using @huggingface/transformers (RMBG-1.4) — no local models needed.");
