# 🔒 تنبيه أمني مهم

> ✅ **تحديث (2026-09-25 — تدقيق `C-02`): لم يعد التطبيق يستخدم أي مفتاح لخادم الذكاء الاصطناعي إطلاقاً.**
> حُذف `ModalAIKey`/`GetModalAIKey` (Go) وعلم `-X grido/internal/service.ModalAIKey` من `build.ps1` و`build/windows/Taskfile.yml` و`.github/workflows/release.yml`،
> والمسار الوحيد للتوثيق الآن هو **JWT المستخدم** (`Authorization: Bearer`) الذي يتحقق منه `modal_ai/upscaler.py` مع Supabase.
> **المطلوب من المالك:** تنفيذ **إبطال (Revoke)** لأي مفتاح Modal قديم يبدأ بـ `grido_sec_ai_live_` — فهو لا يزال قابلاً للاستخراج بـ`strings` من الإصدارات المبنية سابقاً.
> الخطوات أدناه محفوظة كسجل تاريخي للحادثة الأولى.

## المفتاح المُسرب تم إبطاله

تم اكتشاف مفتاح API مكشوف في الكود السابق:
```
grido_sec_ai_live_****************************************************************
```

### الإجراءات المتخذة:
1. ✅ تم حذف المفتاح من الكود المصدري
2. ⚠️ **يجب إبطال هذا المفتاح فوراً من لوحة Modal AI**
3. ✅ تم حذف آلية المفتاح بالكامل من البناء والكود (2026-09-25) — لا يوجد ما يُرفض لغيابه لأن لا مفتاح يلزم
4. ✅ تم تحديث `.env.example` مع تعليمات واضحة (لا مفتاح مطلوب)

### الخطوات المطلوبة للمطورين:

#### 1. إبطال المفتاح القديم
```bash
# قم بتسجيل الدخول إلى لوحة Modal AI
# انتقل إلى API Keys
# ابحث عن المفتاح القديم وقم بحذفه/إبطاله
```

#### 2. إنشاء مفتاح جديد
```bash
# من لوحة Modal AI:
# 1. اضغط "Create New API Key"
# 2. أعطه اسم "Grido Studio Production"
# 3. انسخ المفتاح الجديد
```

#### 3. تحديث ملف .env المحلي
```bash
cp .env.example .env
# ضع المفتاح الجديد في .env
nano .env
```

#### 4. لا حاجة لمفتاح في GitHub Secrets (تحديث 2026-09-25)
```bash
# ملف .github/workflows/release.yml لا يقرأ أي مفتاح Modal بعد الآن (تدقيق C-02).
# لا تضف MODAL_AI_KEY / GRIDO_AI_SECRET_KEY إلى أسرار المستودع.
# المطلوب فقط: SUPABASE_URL و SUPABASE_ANON_KEY.
```

### التحقق من الأمان:

```bash
# تأكد أن .env في .gitignore
grep -q "^\.env$" .gitignore && echo "✅ محمي" || echo "❌ غير محمي"

# تأكد من عدم وجود مفاتيح في الكود
grep -r "grido_sec_ai" internal/ && echo "❌ مفتاح موجود!" || echo "✅ آمن"

# تأكد من وجود .env
[ -f .env ] && echo "✅ .env موجود" || echo "❌ أنشئ .env من .env.example"
```

### قواعد الأمان المستقبلية:

1. ❌ **لا تضع أبداً** مفاتيح API في الكود
2. ✅ **استخدم دائماً** متغيرات البيئة
3. ✅ **تأكد** أن .env في .gitignore
4. ✅ **استخدم** GitHub Secrets للـ CI/CD
5. ✅ **قم بالتدوير** للمفاتيح بانتظام (كل 6 أشهر)
6. ✅ **راقب** الاستخدام الشاذ للـ API

### في حالة تسريب مفتاح:

1. **أبطله فوراً** من لوحة التحكم
2. **أنشئ مفتاح جديد** على الفور
3. **حدّث جميع البيئات** (dev, staging, production)
4. **راجع السجلات** لاكتشاف أي استخدام غير مصرح
5. **أبلغ الفريق** عن الحادثة

---

**تاريخ التنبيه:** 2026-07-26  
**الحالة:** تم الإصلاح ✅  
**المطلوب:** إبطال المفتاح القديم ⚠️
